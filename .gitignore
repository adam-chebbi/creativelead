# ── SECRETS — never commit these ─────────────────────────────
.env
/etc/autoreach.env
*.key               # SSH private keys
*.pem               # SSL certificates / private keys
*.ppk               # PuTTY private keys

# ── PERSONAL DATA — private to each user ──────────────────────
businesses.csv
sent_log.csv
followup_log.csv
autoreach_users.db  # SQLite user database
email_template.txt  # user's custom email template
/data/              # Fly.io / Oracle volume mount

# ── PYTHON ENVIRONMENT ────────────────────────────────────────
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# ── OS JUNK ──────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── EDITOR JUNK ──────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── LEGACY ───────────────────────────────────────────────────
static/style.css
README_DASHBOARD.md

# ── ARIA client-side key (not for public repo) ────────────────
docs/aria-key.js
